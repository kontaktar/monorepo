Please don't write Markdown for every change or fix, 
let's just maintain a single README.md file in the root of the project,
and one in every single app/package.
The README.md should be brief and to the point, describing what the app/package does, how to set it up, and how to run it.
